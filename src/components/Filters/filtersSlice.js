const initState = {
    search:"",
    status:"All",
    priority: "Medium",
}

function todoListSlice (state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default todoListSlice;